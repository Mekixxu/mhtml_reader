# Tasks

- [x] Task 1: Enable Edge-to-Edge and Handle Insets
  - [x] SubTask 1.1: Call `enableEdgeToEdge()` in `MainActivity.onCreate` (requires `androidx.activity:activity-ktx` dependency, check if available or use manual approach).
  - [x] SubTask 1.2: Add `ViewCompat.setOnApplyWindowInsetsListener` to the root view in `MainActivity` to apply padding for system bars.
